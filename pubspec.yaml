name: test_app
description: Changes background to a random RGB color on tap.
publish_to: none
version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  solid_lints: ^0.3.0

flutter:
  uses-material-design: true